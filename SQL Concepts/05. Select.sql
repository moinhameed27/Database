select * from student;

/*
SELECT : selects columns from the table.
* selects all columns
1. If you want to select specific columns, write name of columns.
*/

select rollno, name, grade from student;

/* DISTINCT : Selects only unique data from any column. */
select distinct grade from student;

